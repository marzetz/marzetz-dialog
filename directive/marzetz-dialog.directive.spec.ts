import { MarzetzDialogDirective } from './marzetz-dialog.directive';

describe('MarzetzDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new MarzetzDialogDirective();
    expect(directive).toBeTruthy();
  });
});
