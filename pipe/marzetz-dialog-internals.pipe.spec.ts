import {MarzetzDialogInternalsPipe} from './marzetz-dialog-internals.pipe';

describe('MarzetzDialogInternalsPipe', () => {
    it('create an instance', () => {
        const pipe = new MarzetzDialogInternalsPipe();
        expect(pipe).toBeTruthy();
    });
});
